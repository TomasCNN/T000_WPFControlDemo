<Window x:Class="T007002_WPFLayoutDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T007002_WPFLayoutDemo"
        mc:Ignorable="d" Title="设备列表" Width="800" Height="600">
    <!-- DockPanel：顶部停靠标题栏，左侧停靠分类栏，剩余空间给列表 -->
    <DockPanel>
        <!-- 1. 顶部标题栏（停靠顶部） -->
        <Border DockPanel.Dock="Top" Background="#2196F3" Height="50">
            <TextBlock Text="设备管理系统" FontSize="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>

        <!-- 2. 左侧分类栏（停靠左侧） -->
        <Border DockPanel.Dock="Left" Background="#F0F0F0" Width="150" Margin="0,0,1,0">
            <StackPanel Margin="10">
                <Button Content="全部设备" Height="35" Background="#E3F2FD" />
                <Button Content="CNC设备" Height="35" />
                <Button Content="AOI设备" Height="35" />
                <Button Content="输送线" Height="35" />
            </StackPanel>
        </Border>

        <!-- 3. 右侧设备列表（占满剩余空间） -->
        <ListView x:Name="DeviceListView" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- 列表项：嵌套Grid布局 -->
                    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- 状态指示灯 -->
                            <Ellipse Grid.Column="0" Width="15" Height="15" Fill="{Binding StatusColor}" Margin="0,0,10,0" />

                            <!-- 设备信息 -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding DeviceId}" FontSize="14" FontWeight="Bold" />
                                <TextBlock Text="{Binding DeviceName}" FontSize="12" Foreground="Gray" />
                            </StackPanel>

                            <!-- 运行时间 -->
                            <TextBlock Grid.Column="2" Text="{Binding RunTime}" FontSize="12" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
