<Window x:Class="T007023_WPFLayoutGridSplitterDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T007023_WPFLayoutGridSplitterDemo"
        mc:Ignorable="d" 
        Title="垂直分割示例" Height="450" Width="800">
    <Grid>
        <!-- 定义3列：左栏 + 分割线 + 右栏 -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200*" MinWidth="150" />
            <!-- 左栏，最小150px -->
            <ColumnDefinition Width="Auto" />
            <!-- 分割线列，自动宽度 -->
            <ColumnDefinition Width="600*" MinWidth="300" />
            <!-- 右栏，最小300px -->
        </Grid.ColumnDefinitions>

        <!-- 左栏内容 -->
        <Border Grid.Column="0" Background="#F5F6F8">
            <TextBlock Text="左侧导航栏" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>

        <!-- 垂直分割线（核心） -->
        <GridSplitter Grid.Column="1" Width="5" Background="#DDD" VerticalAlignment="Stretch" HorizontalAlignment="Center" ShowsPreview="True" ResizeBehavior="PreviousAndNext" />

        <!-- 右栏内容 -->
        <Border Grid.Column="2" Background="White">
            <TextBlock Text="右侧内容区" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
    </Grid>
</Window>
