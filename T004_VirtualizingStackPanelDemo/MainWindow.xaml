<Window x:Class="T004_VirtualizingStackPanelDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T004_VirtualizingStackPanelDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ItemsControl x:Name="itemsControl">
            <!-- 1. 开启滚动（虚拟化必须依赖ScrollViewer） -->
            <ItemsControl.Template>
                <ControlTemplate>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>

            <!-- 2. 指定 ItemsPanel = VirtualizingStackPanel -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- 水平排列 + 虚拟化 -->
                    <VirtualizingStackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <!-- 列表项样式 -->
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="80" Height="80" Background="LightBlue" Margin="5" CornerRadius="5">
                        <TextBlock Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
