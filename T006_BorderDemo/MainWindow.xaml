<Window x:Class="T006_BorderDemo.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:T006_BorderDemo" mc:Ignorable="d" Title="工业设备状态卡片" Width="400" Height="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <!-- 设备状态卡片 Border -->
        <Border x:Name="DeviceCardBorder" Width="350" Height="200" Background="#E8F5E9" BorderBrush="#4CAF50" BorderThickness="2" CornerRadius="10" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">

            <!-- 嵌套 Grid 承载内容 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- 状态指示灯 -->
                <Ellipse x:Name="StatusEllipse" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Width="50" Height="50" Fill="#4CAF50" Margin="0,0,20,0" />

                <!-- 设备ID -->
                <TextBlock Grid.Row="0" Grid.Column="1" Text="设备 ID：CNC_001" FontSize="18" FontWeight="Bold" />

                <!-- 状态文本 -->
                <TextBlock x:Name="StatusText" Grid.Row="1" Grid.Column="1" Text="状态：运行中" FontSize="16" Foreground="#4CAF50" Margin="0,5,0,0" />

                <!-- 分隔线 -->
                <Rectangle Grid.Row="2" Grid.ColumnSpan="2" Height="1" Fill="#CCCCCC" Margin="0,15" />

                <!-- 操作按钮 -->
                <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button x:Name="BtnStart" Content="启动" Width="80" Height="30" Margin="0,0,10,0" Click="BtnStart_Click" />
                    <Button x:Name="BtnStop" Content="停止" Width="80" Height="30" Margin="0,0,10,0" Click="BtnStop_Click" />
                    <Button x:Name="BtnFault" Content="模拟故障" Width="80" Height="30" Click="BtnFault_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
