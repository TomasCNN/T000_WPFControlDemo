# 003011020_工业上位机 WPF 项目 文件夹规范完整手册

**版本**：V1.0

**适用范围**：工业视觉检测、自动化设备、PLC 上位机项目

**生效日期**：2026-05-23

**维护团队**：设备软件开发组

------

## 一、总则

### 1.1 规范目的

- 统一团队代码风格，提高代码可读性和可维护性
- 确保项目结构清晰，新人上手时间缩短至 1 天以内
- 实现硬件可插拔，换相机 / PLC 品牌时上层代码零改动
- 避免代码变成 "屎山"，保证项目 5~10 年的生命周期
- 降低团队协作成本，减少代码冲突和沟通成本

### 1.2 核心设计原则

> **分层架构原则**：按职责分层，数字越小层级越低越稳定，上层只能依赖下层
>
> 
>
> **依赖倒置原则**：依赖抽象而非具体实现，所有硬件操作都通过接口进行
>
> 
>
> **单一职责原则**：每个文件夹、每个文件、每个类只做一件事
>
> 
>
> **清晰性优先原则**：命名要能准确反映职责，宁长勿短

------

## 二、项目文件夹规范

### 2.1 整体结构

```tex
IndustrialVisionTemplate/
├── 01_Abstractions/          # 硬件抽象层（最底层）
├── 02_Implementations/        # 硬件实现层
├── 03_Services/               # 服务层
├── 04_Business/               # 业务逻辑层
├── 05_ViewModels/             # MVVM层
├── 06_Views/                  # UI层
├── 07_Models/                 # 公共数据模型层
├── 08_Data/                   # 数据访问层
├── 09_Common/                 # 通用工具层
└── Configs/                   # 配置层
```

### 2.2 各文件夹详细说明

| 文件夹                 | 层级           | 核心作用                                        | 允许的文件类型                             | 绝对禁止放入                             | 线扫视觉设备示例                                             |
| :--------------------- | :------------- | :---------------------------------------------- | :----------------------------------------- | :--------------------------------------- | :----------------------------------------------------------- |
| **01_Abstractions**    | 硬件抽象层     | 定义所有硬件和服务的统一接口，隔离变化          | 接口文件 (.cs)                             | 任何实现类、任何业务逻辑、任何 UI 代码   | `ICamera.cs`、`IPlc.cs`、`ILogger.cs`、`IAlgorithmService.cs` |
| **02_Implementations** | 硬件实现层     | 不同品牌硬件的具体 SDK 调用和寄存器操作         | 接口的具体实现类 (.cs)                     | 任何业务逻辑、任何 UI 代码               | `IkCamera.cs`、`SiemensPlc.cs`、`NLogLogger.cs`、`SurfaceDefectDetector.cs` |
| **03_Services**        | 服务层         | 封装通用能力，提供强类型、安全的 API 给上层调用 | 服务类 (.cs)                               | 任何 UI 代码、任何业务流程               | `CameraService.cs`、`PlcService.cs`、`ConfigService.cs`、`DataService.cs` |
| **04_Business**        | 业务逻辑层     | 核心业务流程编排、业务规则判断、设备状态管理    | 流程类、状态机类、管理器类 (.cs)           | 任何硬件操作、任何 UI 代码、任何数据访问 | `DetectionProcess.cs`、`ProductionManager.cs`、`DeviceStateMachine.cs`、`AlarmManager.cs` |
| **05_ViewModels**      | MVVM 层        | UI 逻辑、数据绑定、命令处理、UI 状态管理        | ViewModel 类 (.cs)                         | 任何硬件操作、任何业务逻辑、任何数据访问 | `MainViewModel.cs`、`MonitorViewModel.cs`、`ParameterViewModel.cs`、`IoMonitorViewModel.cs` |
| **06_Views**           | UI 层          | 界面布局、样式、控件、用户输入处理              | XAML 文件 + 对应的.xaml.cs 代码隐藏文件    | 任何业务逻辑、任何硬件调用、任何数据处理 | `MainWindow.xaml`、`MonitorView.xaml`、`ParameterView.xaml`、`IoMonitorView.xaml` |
| **07_Models**          | 公共数据模型层 | 纯数据结构，无任何逻辑                          | 实体类、DTO 类、枚举 (.cs)                 | 任何方法、任何逻辑、任何依赖             | `Defect.cs`、`ProductRecord.cs`、`AppConfig.cs`、`DeviceStatus.cs` |
| **08_Data**            | 数据访问层     | 数据库、文件、图像的读写操作                    | 数据库上下文、文件管理器、数据仓储类 (.cs) | 任何业务逻辑、任何 UI 代码               | `DatabaseContext.cs`、`FileManager.cs`、`DefectRepository.cs`、`ImageSaver.cs` |
| **09_Common**          | 通用工具层     | 所有模块通用的辅助类、扩展方法、转换器          | 转换器、扩展方法、帮助类、常量类 (.cs)     | 任何业务相关代码                         | `BoolToColorConverter.cs`、`BitmapExtensions.cs`、`ImageHelper.cs`、`TimeHelper.cs` |
| **Configs**            | 配置层         | 所有系统配置文件                                | 配置文件 (.json、.config、.xml)            | 任何代码文件                             | `appsettings.json`、`NLog.config`、`PlcAddresses.json`、`CameraParams.json` |

### 2.3 子文件夹组织规范

- 当某个文件夹下的文件超过 10 个时，必须按功能拆分子文件夹
- 子文件夹使用帕斯卡命名法，复数形式
- 同一层级的文件数量不超过 15 个

**正确示例**：

```tex
02_Implementations/
├── Cameras/                  # 所有相机实现
│   ├── IkCamera.cs
│   ├── HikCamera.cs
│   └── BaslerCamera.cs
├── Plcs/                     # 所有PLC实现
│   ├── SiemensPlc.cs
│   ├── OmronPlc.cs
│   └── MitsubishiPlc.cs
├── Algorithms/               # 所有算法实现
│   ├── SurfaceDefectDetector.cs
│   └── DimensionDetector.cs
└── Loggers/                  # 所有日志实现
    └── NLogLogger.cs
```

------

## 三、文件命名规范

### 3.1 通用规则

- 所有文件使用帕斯卡命名法（PascalCase）
- 文件名必须准确反映文件的职责
- 避免使用缩写，除非是行业通用缩写（如 PLC、IO、UI、SDK）

### 3.2 各文件夹文件命名规则

| 文件夹             | 命名规范                                           | 正确示例                                         | 错误示例                             |
| :----------------- | :------------------------------------------------- | :----------------------------------------------- | :----------------------------------- |
| 01_Abstractions    | `I+功能名称.cs`                                    | `ICamera.cs`、`IPlc.cs`                          | `Camera.cs`、`IIkCamera.cs`          |
| 02_Implementations | `品牌+功能名称.cs`                                 | `IkCamera.cs`、`SiemensPlc.cs`                   | `Camera.cs`、`Plc.cs`                |
| 03_Services        | `功能名称+Service.cs`                              | `CameraService.cs`、`PlcService.cs`              | `CameraManager.cs`、`PlcHelper.cs`   |
| 04_Business        | `功能名称+Process/Manager/StateMachine.cs`         | `DetectionProcess.cs`、`ProductionManager.cs`    | `Business.cs`、`Process.cs`          |
| 05_ViewModels      | `功能名称+ViewModel.cs`                            | `MainViewModel.cs`、`MonitorViewModel.cs`        | `MainVM.cs`、`Monitor.cs`            |
| 06_Views           | `功能名称+View.xaml`                               | `MonitorView.xaml`、`ParameterView.xaml`         | `Monitor.xaml`、`ParameterPage.xaml` |
| 07_Models          | `实体名称.cs`                                      | `Defect.cs`、`ProductRecord.cs`                  | `DefectModel.cs`、`Product.cs`       |
| 08_Data            | `功能名称+Context/Manager/Repository.cs`           | `DatabaseContext.cs`、`FileManager.cs`           | `Data.cs`、`DBHelper.cs`             |
| 09_Common          | `功能名称+Converter/Extension/Helper/Constants.cs` | `BoolToColorConverter.cs`、`BitmapExtensions.cs` | `Converter.cs`、`Helper.cs`          |

------

## 四、代码命名规范

### 4.1 通用规则

- 类、接口、方法、属性、事件使用帕斯卡命名法
- 私有字段使用驼峰命名法（camelCase），前缀下划线
- 常量使用全大写，下划线分隔
- 布尔值必须使用 is/has/can 前缀

### 4.2 详细规则

| 类型     | 规范                              | 正确示例                               | 错误示例                          |
| :------- | :-------------------------------- | :------------------------------------- | :-------------------------------- |
| 类       | 帕斯卡命名法，名词或名词短语      | `CameraService`、`DetectionProcess`    | `cameraService`、`process`        |
| 接口     | 前缀 I + 帕斯卡命名法             | `ICamera`、`IPlc`                      | `Camera`、`CameraInterface`       |
| 方法     | 帕斯卡命名法，动词或动词短语      | `GetBitmap()`、`WaitForStartTrigger()` | `getBitmap()`、`trigger()`        |
| 属性     | 帕斯卡命名法，名词或名词短语      | `ExposureTimeUs`、`IsRunning`          | `exposureTime`、`running`         |
| 事件     | 帕斯卡命名法，动词 + 过去分词后缀 | `DefectDetected`、`ProcessCompleted`   | `DetectDefect`、`CompleteProcess` |
| 私有字段 | 下划线前缀 + 驼峰命名法           | `_camera`、`_plcService`               | `camera`、`m_plc`                 |
| 常量     | 全大写，下划线分隔                | `MAX_TIMEOUT`、`DEFAULT_EXPOSURE`      | `MaxTimeout`、`defaultExposure`   |
| 布尔值   | is/has/can 前缀 + 帕斯卡命名法    | `IsRunning`、`HasDefect`、`CanStart`   | `Running`、`Defect`、`Start`      |

### 4.3 工业场景特殊命名规范（必须遵守）

1. **硬件相关命名必须带品牌前缀**

   

   csharp：

   ```c#
   // 正确
   public class IkCamera : ICamera { }
   public class SiemensPlc : IPlc { }
   
   // 错误
   public class Camera : ICamera { }
   public class Plc : IPlc { }
   ```

   

2. **PLC 地址绝对不能用魔法字符串，必须定义常量集中管理**

   

   csharp：

   ```c#
   // 正确
   public static class PlcAddressConstants
   {
       public const string START_TRIGGER = "M100";
       public const string END_TRIGGER = "D200";
       public const string ALARM = "M200";
       public const string BARCODE = "DB1.DBW0";
   }
   
   // 调用
   _plc.WriteShort(PlcAddressConstants.END_TRIGGER, 3);
   
   // 错误
   _plc.WriteShort("D200", 3);
   ```

   

3. **事件命名必须用过去分词，表示已发生的事件**

   csharp:

   ```c#
   // 正确
   public class DefectDetectedEvent : PubSubEvent<List<Defect>> { }
   public class ProcessCompletedEvent : PubSubEvent<bool> { }
   
   // 错误
   public class DetectDefectEvent : PubSubEvent<List<Defect>> { }
   public class CompleteProcessEvent : PubSubEvent<bool> { }
   ```

   

4. **时间单位必须在变量名中明确说明**

   

   csharp:

   ```c#
   // 正确
   public int ExposureTimeUs { get; set; } // 微秒
   public int TimeoutMs { get; set; }     // 毫秒
   public int LineSpeedMpm { get; set; }  // 米/分钟
   
   // 错误
   public int ExposureTime { get; set; }  // 不知道单位是微秒还是毫秒
   ```

   

------

## 五、最佳实践

### 5.1 项目结构最佳实践

1. **严格执行文件位置检查**：代码审查第一关就是检查文件是否放在了正确的文件夹中
2. **新人培训第一节课讲项目结构**：让每个新人在写第一行代码之前就清楚项目结构
3. **不要随意新增根文件夹**：如需新增，必须经过团队讨论
4. **定期清理无效文件**：删除不再使用的文件和代码，保持项目整洁
5. **所有配置都放在 Configs 文件夹**：绝对不要在代码中硬编码任何参数

### 5.2 代码编写最佳实践

1. **所有硬件操作都通过接口进行**：上层代码永远不要直接依赖具体硬件实现
2. **所有公共方法都要做参数校验**：防止非法输入导致系统崩溃
3. **所有硬件操作都要加超时和异常处理**：工业软件必须有完善的错误处理机制
4. **所有非托管资源都要实现 IDisposable 接口**：防止内存泄漏
5. **所有事件订阅都要保存订阅令牌并在适当的时候取消**：防止内存泄漏

### 5.3 团队协作最佳实践

1. **提交代码前先运行单元测试**：确保代码不会破坏现有功能
2. **每次提交只做一件事**：提交信息要清晰准确，说明做了什么修改
3. **代码审查必须覆盖所有修改**：至少有一个其他团队成员审查代码
4. **定期组织代码评审会**：分享好的代码，指出存在的问题
5. **持续更新规范文档**：当团队达成新的共识时，及时更新规范文档

------

## 六、违规示例与正确示例对比

### 6.1 文件夹位置错误

❌ 错误：把`IkCamera.cs`放在`03_Services`文件夹中

✅ 正确：把`IkCamera.cs`放在`02_Implementations/Cameras`文件夹中

### 6.2 文件命名错误

❌ 错误：`Camera.cs`

✅ 正确：`IkCamera.cs`

### 6.3 代码命名错误

❌ 错误：

csharp:

```c#
public class cameraService
{
    public Bitmap getBitmap(int timeout)
    {
        // ...
    }
}
```

✅ 正确：

csharp:

```c#
public class CameraService
{
    public Bitmap GetBitmap(int timeoutMs)
    {
        // ...
    }
}
```

### 6.4 硬编码魔法字符串

❌ 错误：

csharp:

```c#
_plc.WriteShort("D200", 3);
```

✅ 正确：

csharp:

```c#
public static class PlcAddressConstants
{
    public const string END_TRIGGER = "D200";
}

_plc.WriteShort(PlcAddressConstants.END_TRIGGER, 3);
```

------

## 七、代码审查快速检查表

-  文件是否放在了正确的文件夹中？
-  文件名是否符合命名规范？
-  类、方法、属性命名是否符合规范？
-  是否有硬编码的魔法字符串？
-  所有硬件操作是否都通过接口进行？
-  所有公共方法是否都做了参数校验？
-  所有硬件操作是否都加了超时和异常处理？
-  所有非托管资源是否都实现了 IDisposable 接口？
-  所有事件订阅是否都有对应的取消订阅？

------

## 八、版本历史

| 版本 | 日期       | 修改内容                                             | 修改人         |
| :--- | :--------- | :--------------------------------------------------- | :------------- |
| V1.0 | 2026-05-23 | 初始版本，包含文件夹规范、文件命名规范、代码命名规范 | 设备软件开发组 |

------

## 针对埃科线扫相机 + 西门子 PLC 项目的补充说明

### 1. 相机参数命名规范

csharp:

```c#
// 正确
public class CameraConfig
{
    public int ExposureTimeUs { get; set; } = 1000;    // 曝光时间(微秒)
    public double Gain { get; set; } = 1.0;            // 增益
    public int LineSpeedKhz { get; set; } = 40;        // 行频(kHz)
    public int TriggerDelayUs { get; set; } = 0;       // 触发延迟(微秒)
}
```

### 2. 图像保存路径规范

```tex
Images/
├── 20260523/
│   ├── OK/
│   │   ├── 20260523100000001.bmp
│   │   └── 20260523100000002.bmp
│   └── NG/
│       ├── 20260523100000003.bmp
│       └── 20260523100000004.bmp
```

### 3. 日志级别规范

- **Debug**：调试信息（如取图耗时、算法耗时）
- **Info**：正常流程信息（如系统启动、检测完成）
- **Warn**：警告信息（如 PLC 写入重试、相机帧率过低）
- **Error**：错误信息（如相机取图超时、PLC 连接失败）