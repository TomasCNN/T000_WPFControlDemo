<Window x:Class="T007001_WPFLayoutDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T007001_WPFLayoutDemo"
        mc:Ignorable="d"
        Title="设备登录" Width="400" Height="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <!-- 外层Border：添加圆角和阴影 -->
    <Border Background="#F5F5F5" CornerRadius="10" Margin="10">
        <!-- 3行1列Grid：划分标题区、输入区、按钮区 -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 1. 标题区 -->
            <TextBlock Grid.Row="0" Text="工业设备管理系统" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" />

            <!-- 2. 输入区：垂直StackPanel -->
            <StackPanel Grid.Row="1">
                <!-- 用户名行：水平StackPanel -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="用户名：" Width="60" FontSize="14" />
                    <TextBox Width="200" Height="30" FontSize="14" />
                </StackPanel>

                <!-- 密码行 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="密  码：" Width="60" FontSize="14" />
                    <PasswordBox Width="200" Height="30" FontSize="14" />
                </StackPanel>
            </StackPanel>

            <!-- 3. 按钮区：水平StackPanel，右对齐 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"  Margin="0,20,0,0">
                <Button Content="登录" Width="80" Height="35" Background="#2196F3" Foreground="White" />
                <Button Content="取消" Width="80" Height="35" />
                <Button Content="注册" Width="80" Height="35" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
